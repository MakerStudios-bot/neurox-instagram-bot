[build]
builder = "nixpacks"

[deploy]
startCommand = "cd vendedor && gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --access-logfile -"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyMaxRetries = 5
numReplicas = 1
