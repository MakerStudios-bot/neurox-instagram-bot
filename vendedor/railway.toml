[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --access-logfile -"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyMaxRetries = 5
numReplicas = 1
