[build]
builder = "nixpacks"
buildCommand = "cd vendedor && pip install -r requirements.txt"

[deploy]
startCommand = "cd vendedor && python app.py"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyMaxRetries = 5
numReplicas = 1
